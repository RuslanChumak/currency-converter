const config = {
  baseUrl: 'https://cdn.jsdelivr.net/npm/@fawazahmed0/currency-api@latest/v1',
  preferredCurrencies: ['usd', 'eur'],
  converterCurrencies: ['usd', 'eur', 'uah']
};

export default config;
